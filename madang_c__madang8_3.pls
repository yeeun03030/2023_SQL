DECLARE
  AVERAGEVAL NUMBER;
BEGIN

  AVERGEPRICE(
    AVERAGEVAL => AVERAGEVAL
  );
  /* Legacy output: 
DBMS_OUTPUT.PUT_LINE('AVERAGEVAL = ' || AVERAGEVAL);
*/ 
  :AVERAGEVAL := AVERAGEVAL;
--rollback; 
END;
